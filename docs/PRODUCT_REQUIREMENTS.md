# 产品需求基线

## 产品定位

xTranslator 是一个 BYOK（用户自带翻译服务密钥）的 Chrome Manifest V3 扩展。它在 YouTube 普通视频与 Shorts 页面上提供语境翻译，不篡改 YouTube 源内容：字幕由自绘叠加层在视觉上“取代”原生字幕，但原始字幕与数据始终保留，可随时切回。

> 字幕译文可读性约定（2026-08-25）：译文保留自然标点，用于表达停顿、疑问和语气；中文正文不人为插入词间空格，英文、数字和专有名词之间的有效空格保留。仅清理字幕标记和装饰性噪声。

## 语言解析

目标语言**不提供手动设置**，始终按浏览器界面语言自动解析，并在本次任务中固定：

1. `chrome.i18n.getUILanguage()`；
2. `navigator.languages[0]`；
3. `navigator.language`；
4. `en`。

解析结果必须规范化为 BCP-47 标签。中文需要保留文字体系：`zh-CN` 映射为 `zh-Hans`，`zh-TW` 映射为 `zh-Hant`。缓存键保存解析后的实际目标语言标签，而不是 `auto`。Popup 顶部显示“翻译语言”，详情为“自动跟随浏览器语言 · <语言名称>”，不直接展示 BCP-47 标签。

## 视频翻译

- 读取视频 ID、标题、简介、时长与可用字幕轨道。
- 当前由播放器字幕轨道列表选择默认源轨道：优先首个非 `asr` 轨道，否则选择首个可用轨道；当前版本尚未提供手动选择源字幕轨道的 UI。翻译请求和缓存都必须绑定实际轨道的 `vssId`、`languageCode`、`kind` 指纹，不能只按视频 ID 判断。
- 播放器翻译支持普通视频和 Shorts。普通视频的按钮只根据**当前视频**判断：只要当前视频有字幕轨道，无论 YouTube 原生字幕按钮是开启、关闭还是不可用，都必须显示；仅当字幕轨道为空时，才使用明确的 DOM 状态作为兜底：字幕按钮不含 `aria-disabled="true"`、`disabled` 属性或 `ytp-button-disabled` class，且 `aria-pressed="true"` 或含 `ytp-button-pressed` class。字幕轨道为空且不满足该条件时不显示。YouTube 的全局“默认开启字幕”偏好不改变无字幕视频的结果。Shorts 使用相同的字幕可用性判断，但播放器按钮由 `settings.subtitles.shortsTranslationEnabled` 控制，默认 `false`。
- 字幕只使用 MAIN world bridge 捕获的 YouTube 播放器真实 `fmt=json3` 响应；不读取 `.srt`/`.txt`，也不直接请求临时 `baseUrl`。
- 将 JSON3 解析为保留 `tStartMs`、`dDurationMs` 和 fragment offset 的时间片段；同一事件中的多句先按句末标点拆开，再按**自然停顿（默认 500ms）、16 秒硬时长上限、token 预算与可读性上限**合并成“翻译块”。相同开始时间保持原始事件顺序；重叠重复事件归一化，不改写不同事件的原始时间；标记与前一条字幕重叠时不强制切块；短的无标点续句在缺口、token 和总时长都安全时合并。字符上限只作为可读性保护，不单独按固定字符数截断完整原始事件。显示层仅裁短前一条的视觉重叠，避免后一条字幕晚于音频出现；最终显示可在背景框内换行，但不得截断。
- LLM 负责：**把碎片合并成通顺的字幕行**、**去掉 `[music]`/`[applause]`/音符等无意义标签**、修正明显 ASR 断句与同音误识；所有目标语言保留自然标点，清理字幕标记和装饰性噪声；中文正文不人为插入词间空格，英文、数字和专有名词之间的有效空格保留；自然口语化，保留说话人语气，避免书面腔；不得借用相邻 block、虚构内容或改变事实。无语音的块不调用 LLM、不显示字幕。
- 译文按**稳定 block ID 回填到同一时间范围**，不按模型返回顺序匹配，时间戳由程序控制；翻译完成后在观看页字幕位置以字幕叠加层显示（激活时持续隐藏原生字幕窗口以在视觉上“取代”它），不在标题/简介区域额外渲染译文面板。只要当前视频存在字幕轨道，即使原生字幕按钮关闭或不可用，也显示翻译按钮；仅当 `captionTracks` 为空且原生字幕按钮不满足上一条定义的 DOM 条件时才不显示。按钮首次显示前等待播放器状态短暂稳定；同一视频后续出现临时不完整响应时不移除已显示按钮。
- LLM 的输入单位是一个包含多个 `TranslationBlockInput` 的批次，而不是逐条 JSON3 原始事件或逐句单独调用；每个 block 保持独立的 `id`、源文和时间边界，模型只返回对应 `id` 的译文，程序负责按 ID 回填。
- 字幕提供**原文、译文、双语**三种模式（设置页选择，默认双语）；双语为**译文在上（默认黄色 `#ffd438`）、原文在下（默认 `#ececf0`）**。字幕层以视频实际显示区域为基准，使用一个紧凑背景卡片，译文为主层级、原文为辅助层级，单行优先、过长自动换行，不能超出屏幕或背景框。默认位置必须在 YouTube 进度条上方并保留 8px 间距；用户可在播放器内上下拖动字幕以避开视频自带文字，保存的位置按播放器高度比例恢复。模式、颜色、字号或位置保存后，正在播放的视频字幕层立即重绘；恢复字幕默认设置会恢复颜色、字号和自动位置，但不改变显示模式。Shorts 字幕相对 `#shorts-player` 定位，原文固定为 15px、译文固定为 19px，不使用普通视频的字号滑块。
- 非静音 block 的空译文视为失败：会重试、不会写入有效缓存；旧缓存中的空译文会重新翻译。多次仍缺失时显示可恢复错误，不伪装成翻译成功。
- 支持短视频与分块长视频；长视频按块分批请求，并尽量命中缓存。每个已完成批次立即写入缓存；超时或部分失败时继续显示已完成块，播放器按钮明确提示“重试未完成翻译”，再次点击只请求缺失 block。
- Provider preset 可声明模型级最大输出 token；字幕请求不得超过该上限，不能把大上下文窗口直接当作输出上限。

## 评论翻译

- Shorts 评论面板与普通视频评论都提供“翻译可见评论”和已展开回复的“翻译这组评论”。`settings.subtitles.shortsTranslationEnabled` 只控制 Shorts 播放器按钮，不得隐藏、移除或禁用评论翻译入口。
- 主入口（翻译可见评论）只翻译当前已由 YouTube 渲染并显示的评论，绝不因翻译而展开折叠回复。按钮随滚动和 YouTube 懒加载重新锚定到当前视口内第一个完整可见的顶级评论；后续评论进入视口后仍可从该入口翻译。若翻译失败，按钮显示“重试”；点按会重试当前可见评论中失败或尚未翻译的部分。
- 当父评论的回复已展开时，显示完整可点击的“翻译这组评论”按钮（图标和文字都可点），将父评论及当前已展开的全部回复送入同一请求。展开/收起回复始终由 YouTube 自己的控件负责。
- 不主动请求或伪造 YouTube 内部 continuation 接口来提前获取下一页、折叠回复或隐藏评论（仅点按页面自己的“查看 N 条回复 / 查看回复”控件，展开后按“已渲染”处理）。
- 使用 token 上限批处理；滚动导致的新评论仅在进入可见区或用户主动翻译时入队。
- 评论上的重复“翻译”入口已精简：仅保留“翻译可见评论”（评论区头部）与已展开回复的父评论上的“翻译这组评论”，避免与 YouTube 自身的回复展开控件混淆。

## 划词翻译

- 划词翻译默认开启，可在设置页关闭；启用时选中网页文本后显示轻量浮动操作，同时提供右键菜单入口。
- 连续点击、双击或拖动选区时，浮层等待约 100ms 后只按最终选区显示，不能先为中间选中的单词弹出翻译浮层。
- 默认只发送选区；可选“附带前后一句上下文”。
- 结果可复制和关闭，不能改变被选中的原始文本节点。

## 缓存与隐私

- 缓存存入 IndexedDB，包含视频标题和完整定时翻译块（源文、译文、时间范围、segment IDs）；不包含短期有效的字幕 URL、LLM Key 或完整请求日志。数据库 schema 当前为 v4，升级时清理不兼容的旧缓存结构。
- 视频缓存按 `videoId` 保存单条记录；记录同时保存 `sourceTrackFingerprint`、`sourceLanguage` 与 `targetLanguage`，读取和增量续译时必须全部匹配，否则视为未命中并替换旧记录。
- 缓存保存完整的定时翻译块；已完成批次会增量写入，空译文不作为有效缓存命中。
- 支持按视频（标题列出）删除、全部删除与占用空间展示；缓存列表以“标题 + `源→目标 · 块数 · 时间`”两行排版。
- API Key 只存于扩展本地存储；不得使用 `chrome.storage.sync`、DOM 属性、页面脚本变量或控制台日志。
- 设置页的翻译服务配置采用“服务商 + 服务密钥 + 翻译模型”三项原子提交：只选择服务商或修改服务密钥时保留为页面草稿，不持久化，也不弹出全局保存错误；服务密钥变更后必须重新加载模型，模型列表加载成功并选定模型后自动保存。每个服务商分别记忆自己的服务密钥和模型，切换到已配置服务商时恢复其配置并自动保存当前服务商。字幕模式、原文/译文颜色、原文/译文字号和拖动位置、Shorts 播放器翻译开关、划词翻译开关和划词上下文选项独立自动保存；Shorts 开关默认关闭且不影响评论翻译。页面不提供“保存更改”按钮。翻译服务加载和配置保存状态优先以内联状态反馈，只有真正的保存失败等异常才使用全局提示。

## LLM 接入

供应商配置分为 OpenAI-compatible、Anthropic Messages 与 Provider preset。Provider 的 Base URL、模型列表和能力必须来自版本化配置或运行时模型发现，不能散落在 UI 或业务代码里。

- 除 Agnes AI 使用版本化静态模型配置外，模型列表通过各 provider 的 `GET /models`（OpenAI-compatible 为 `/models` 或 `/v1/models`，Anthropic 为 `/v1/models`）**运行时拉取**，设置页据此生成模型下拉；保留当前仍存在的模型，否则选择接口返回的第一项。无 API Key 或拉取失败时如实提示，**不编造模型列表**。
- 视频翻译的每个批次只发送视频标题作为参考，不发送视频简介；字幕块、标题和源/目标语言仍按批次完整传入。
- 视频翻译缓存不区分服务商、模型、Prompt 版本、标题或简介；已缓存或当前请求已完成的翻译不因切换模型而重翻，新的翻译请求读取切换后的模型。
- Token 限制只使用已核实的具体模型资料；运行时发现但没有对应限制资料的新模型不继承其他模型限制，不发送猜测的 `max_tokens`，仅使用本地安全分块策略。
- 所有请求必须经过统一适配器，返回统一的结构化翻译结果（块级 `{id, text}`）。源文本被视为不可信输入；系统提示词必须要求模型忽略源文本中的任何指令，并合并碎片、去除非语言标签。
- 失败原因映射为面向用户的中文提示（Key 无效 / 限流 / 超时 / 网络 / 模型不可用 / 服务商异常），日志不含 Key 与请求/响应正文。
